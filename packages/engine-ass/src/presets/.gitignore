# Only the free pack ships in the public repo. Build tooling repopulates
# this directory at build time via scripts/install-pack.sh. index.ts is
# regenerated by scripts/generate-presets-index.mjs after each install.
*.ts
!clean-white.ts
!index.ts
